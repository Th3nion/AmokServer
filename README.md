AmokServer
======

Usefull commands
------

To install all libs use:

```npm install```

To run AmokServer with dev profile:

```npm run dev```

To run AmokServer on production ! :O

```npm run prod```